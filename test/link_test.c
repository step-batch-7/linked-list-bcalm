#include "../list.h"

int main(void){
  char result;
  List_ptr list = malloc(sizeof(List));
  list->head->value = 5;
  list->head->next = 


  printf("%c Should insert into the middle of the list.\n",result);
  return 0;
}